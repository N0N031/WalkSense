Place here the extracted icon files from the provided ZIP packs.

Expected file paths (examples):

- Android adaptive foreground: assets/images/icons/android/adaptive-foreground.png
- Android adaptive background (preferred flat color handled in app.json): assets/images/icons/android/adaptive-background.png (optional)
- Android production store icon (512): assets/images/icons/android/playstore-icon-512.png

- iOS app icon (single best-quality PNG/PNG sets): assets/images/icons/ios/appicon.png

Notes:

- Extract the contents of WalkSense_Android_Icons.zip into assets/images/icons/android.
- Extract the contents of WalkSense_iOS_Icons.zip into assets/images/icons/ios.
- Ensure filename matches those listed above or update app.json accordingly.
- After copying, rebuild or run `npx expo start` and re-run native build if needed (prebuild/eas).
